/// Fuchsbau — the shared design system for the fuchs family of apps.
///
/// The look is **pinned** so the apps are visibly one family, distinguished by
/// icon and content rather than palette or type. See `DESIGN.md`, `ETHOS.md`,
/// and `TECH_STACK.md` in this repo for the full specification.
library;

export 'src/choice_picker.dart';
export 'src/colors.dart';
export 'src/fonts.dart';
export 'src/status_colors.dart';
export 'src/theme.dart';
