name: fuchsbau
description: >
  Shared design system for the fuchs family of apps — the pinned tangerine
  triad palette, theme builder, fonts, and status colours. See DESIGN.md /
  ETHOS.md / TECH_STACK.md in this repo for the specification.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
